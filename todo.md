# TODO — Candela (2026-09-12)

- None required. v0.1.4 is live on PyPI with the persistent-daemon fix and README conflict note;
  CI + auto-release workflows are armed. Known limitation (gamma-tool conflict) is documented.

## Backlog (optional, when time allows)
- None urgent. If ever needed: publish v0.1.5+ via `git tag v<ver> && git push origin <ver>` (auto).
- Consider a "detect competing gamma tool and warn" feature if user-confusion resurfaces.